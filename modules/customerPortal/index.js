const controller = require('./customerPortal.controller');
const service = require('./customerPortal.service');

const repository = require('./customerPortal.repository');

module.exports = { controller, service, repository };
